# Lokasi database SQLite (dibuat otomatis oleh `bun run db:push`)
DATABASE_URL=file:./db/custom.db

# Opsional — kunci enkripsi internal (WAJIB diganti di produksi!)
# Dipakai untuk mengenkripsi cermin kata sandi login/penarikan.
HAKOX_SECRET_KEY=ganti-dengan-kunci-rahasia-anda

# Opsional — kunci token sesi panel admin (WAJIB diganti di produksi!)
ADMIN_SECRET=ganti-dengan-kunci-admin-anda
